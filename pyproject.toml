[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "func-forger"
version = "0.1.0"
description = "Human drafts function skeletons bottom-up; an LLM implements, stores, and reuses them."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Func-Forger" }]
dependencies = [
    "anthropic>=0.40",
    "openai>=1.40",
]

[project.optional-dependencies]
ui = ["prompt_toolkit>=3.0", "rich>=13.0"]
dev = ["pytest>=8.0"]

[project.scripts]
forger = "forger.cli:main"

[tool.setuptools.packages.find]
include = ["forger*"]
